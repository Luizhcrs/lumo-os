[package]
name = "lumo-ipc"
version = "0.1.0"
edition = "2021"
description = "Lumo OS IPC types (lumo-wm <-> lumo-bar over unix socket)."

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
