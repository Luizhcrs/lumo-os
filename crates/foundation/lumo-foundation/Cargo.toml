[package]
name = "lumo-foundation"
version = "0.1.0"
edition = "2021"
description = "Lumo Foundation: design tokens, color helpers, geometry primitives."

[lib]
path = "src/lib.rs"

[dependencies]
notify = "6"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
