[package]
name = "lumo-error"
version = "0.1.0"
edition = "2021"
description = "Taxonomia + crash dump compartilhado para todo workspace Lumo OS."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
backtrace = "0.3"
