[package]
name = "lumoctl"
version = "0.1.0"
edition = "2021"
description = "Lumo OS control CLI: theme, workspaces, shell commands."

[[bin]]
name = "lumoctl"
path = "src/main.rs"

[dependencies]
lumo-ipc = { path = "../../compositor/lumo-ipc" }
lumo-foundation = { path = "../../foundation/lumo-foundation" }
serde_json = "1"
